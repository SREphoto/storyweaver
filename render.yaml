services:
  - type: web
    name: storyweaver-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run start-server
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: GEMINI_API_KEY
        sync: false
      - key: JWT_SECRET
        generateValue: true
